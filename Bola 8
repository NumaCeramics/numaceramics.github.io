<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Bola 8 Mágica</title>

  <!-- Fuente VT323 -->
  <link href="https://fonts.googleapis.com/css2?family=VT323&display=swap" rel="stylesheet">

  <style>
    :root {
      --max-width: 700px;
    }

    body {
      margin: 0;
      padding: 0;
      background: black;
      color: white;
      font-family: 'VT323', monospace;
      display: flex;
      flex-direction: column;
      align-items: center;
      min-height: 100vh;
    }

    h1 {
      margin-top: 5vh;
      font-size: min(20vw, 90px);
      text-align: center;
    }

    /* CONTENEDOR */
    .container {
      position: relative;
      width: min(95vw, var(--max-width));
      aspect-ratio: 1/1;
      margin-top: 3vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    /* TRIÁNGULO */
    .triangle {
      position: absolute;
      width: 90%;
      height: 90%;
      image-rendering: optimizeQuality;
      shape-rendering: geometricPrecision;
    }

    /* RESPUESTA */
    .answer {
      position: absolute;
      width: 60%;
      height: 60%;
      top: 52%;
      left: 50%;
      transform: translate(-50%, -50%);
      color: white;
      font-size: min(25vw, 75px);
      text-align: center;
      display: flex;
      justify-content: center;
      align-items: flex-start;
      padding-top: 35%;
      pointer-events: none;
      white-space: pre-wrap;
      text-shadow: 0 0 8px #0030ff;
      opacity: 0;
    }

    /* Animación aparición */
    @keyframes fadeInScale {
      0% { opacity: 0; transform: translate(-50%, -50%) scale(0.6); }
      100% { opacity: 1; transform: translate(-50%, -50%) scale(1); }
    }
    .animate { animation: fadeInScale 0.6s ease forwards; }

    /* Temblor */
    @keyframes shake {
      0%, 100% { transform: translate(0, 0); }
      20% { transform: translate(-10px, 5px); }
      40% { transform: translate(10px, -6px); }
      60% { transform: translate(-8px, -4px); }
      80% { transform: translate(7px, 8px); }
    }
    .shaking { animation: shake 0.6s ease-in-out; }

    button {
      margin-top: 5vh;
      padding: 2vh 8vh;
      font-size: min(12vw, 55px);
      font-family: 'VT323', monospace';
      background: black;
      color: white;
      border: 4px solid white;
      cursor: pointer;
    }

    .footer {
      margin-top: 5vh;
      margin-bottom: 4vh;
      font-size: min(20vw, 55px);
    }

    .footer a {
      color: white;
      text-decoration: underline;
    }
  </style>
</head>

<body>

  <h1>Bola 8 Mágica</h1>

  <div class="container" id="bola">
    <svg class="triangle" viewBox="0 0 100 100">
      <polygon points="50,100 0,14 100,14" fill="#2749F5"/>
    </svg>
    <div class="answer" id="answer"></div>
  </div>

  <button onclick="activarBola8()">Agitar</button>

  <div class="footer">
    By: <a href="https://www.instagram.com/numa.bombareli/" target="_blank">numa.bombareli</a>
  </div>

  <!-- 🔊 AUDIO -->
  <audio id="misterySound">
    <source src="mystery.mp3" type="audio/mpeg">
  </audio>

  <script>
    const respuestas = [
      "🖕",
      "TÚ SABRÁS",
      "NOO",
      "NAAH",
      "¿ENSERIO?",
      "NO SE JAJA",
      "SIN DUDA",
      "SIII",
      "QUE VA",
      "PIENSA",
      "YA VES"
    ];

    const bola = document.getElementById("bola");
    const texto = document.getElementById("answer");
    const sonido = document.getElementById("misterySound");

    function activarBola8() {
      texto.textContent = "";
      texto.classList.remove("animate");
      texto.style.opacity = 0;

      // 1. TIEMBLA
      bola.classList.add("shaking");

      // 2. PARA de temblar y entonces suena + aparece respuesta
      setTimeout(() => {
        bola.classList.remove("shaking");

        // AUDIO sincronizado al parar
        sonido.currentTime = 0;
        sonido.play().catch(()=>{});

        // Generar respuesta
        const respuesta = respuestas[Math.floor(Math.random() * respuestas.length)];
        texto.textContent = respuesta;

        // Animación
        void texto.offsetWidth;
        texto.classList.add("animate");

      }, 600); // <- el tiempo del temblor
    }

    activarBola8();
  </script>

</body>
</html>
